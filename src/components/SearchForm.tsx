import React from "react"
import { Input, Button } from "./UI"

const SearchForm = () => {
  return (
    <form>
      <Input>Article title</Input>
      <Button>Search</Button>
    </form>
  )
}

export default SearchForm
